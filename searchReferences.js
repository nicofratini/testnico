import fs from 'fs'; import path from 'path'; const searchKeywords = ['calculateFinalCapital', 'initialCapital', 'leverageAmount']; const dir = 'src'; const searchFiles = (dir, keywords) => { const files = fs.readdirSync(dir); files.forEach((file) => { const filePath = path.join(dir, file); if (fs.statSync(filePath).isDirectory()) { searchFiles(filePath, keywords); } else { const content = fs.readFileSync(filePath, 'utf8'); keywords.forEach((keyword) => { if (content.includes(keyword)) { console.log('Keyword \'' + keyword + '\' found in ' + filePath); } }); } }); }; searchFiles(dir, searchKeywords);
